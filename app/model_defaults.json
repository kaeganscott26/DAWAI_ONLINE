{
  "default_model": "openai:gpt-5",
  "available_models": [
    "openai:gpt-5",
    "openai:gpt-5-mini",
    "local:llama3-instruct"
  ],
  "notes": "Phase 1 uses provider stubs for local routing. Set OPENAI_API_KEY to enable OpenAI responses."
}
